import styled from "styled-components"

export const EmptyDataMessage = styled.p`
  font-size: 1.2rem;
  font-weight: 500;
  text-align: center;

  @media (width >= 768px) {
    font-size: 1.4rem;
  }
`