import styled from "styled-components";

export const Button = styled.button`
  height: 4.3rem;
  background-color: var(--gray-400);
  border-radius: 5px;
  border: none;
  color: var(--white);
  cursor: pointer;

  padding: 1rem;
`